[build]
  publish = "."

[functions]
  directory = "netlify/functions"

# Keep repo tooling and source out of the deployed site
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
